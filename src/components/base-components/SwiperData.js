
import React from "react";
import { Navigation, Pagination, Scrollbar, A11y } from "swiper";
import { Swiper, SwiperSlide } from "swiper/react";

const SwiperData = () => {
  

  return (
    <main>
      {/* <div className="mt-20 px-6 flex flex-row items-center lg:w-2/5">
        {sliderData.map((x, index) => {
          return (
            <div key={index}>
              <img alt="" src={x.image} />
              <div className="mb-5">
                <h2 className="font-semibold text-masto-brown-10 text-2xl text-center my-5">
                  {x.title}
                </h2>
                <p className="text-masto-ash-10 text-center text-sm">
                  {x.context}
                </p>
              </div>
            </div>
          );
        })}
      </div> */}
    </main>
  );
};

export default SwiperData;
